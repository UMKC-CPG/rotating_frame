"""The example run files, packaged so that they reach every user.

This directory holds data, not code: each `*.toml` file here is a
complete, ready-to-run run file. It is a package only so that the
files are installed along with the library and can be found through
it (`rotating_frame.cli.support.example_files`), which is what lets
`rfsim circle` and `rfsim --examples` work identically in a clone,
in a shared `physdemo` suite, and in a `pip`-installed copy (physdemo
contract C8). The repository's top-level `runs` is a symbolic link to
this directory.

Attribution: this module is part of the Rotating Frame teaching tool.
"""
