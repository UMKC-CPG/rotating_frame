"""The physics of the Rotating Frame tool.

PLACEHOLDER. The skeleton ships uniform circular motion so that the
tool has a run, a scene, and an oracle test from its first commit
(physdemo DESIGN 4). The tool's own DESIGN and PSEUDOCODE replace
this subpackage; nothing outside `cli/` and `tests/` depends on the
placeholder's name.

Attribution: this module is part of the Rotating Frame teaching tool.
"""
