"""Presentation: the offscreen rule, the palettes, and the vedo scene.

`offscreen.py` is inherited from the physdemo skeleton and must stay
free of vtk and vedo imports, because its one function runs before
either is imported. `vedo_renderer.py` is the only module in the tool
that imports vedo; a different backend is a new module here and
nothing else changes.

Attribution: this module is part of the Rotating Frame teaching tool.
"""
